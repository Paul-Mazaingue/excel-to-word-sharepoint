
git clone
sudo docker-compose up -d

ETAPE RCLONE

installation via :
curl https://rclone.org/install.sh | sudo bash

Configuration :
rclone config
n
name: sharepoint
36
Default until auth
Auth -> use web if you can else use the other one
4 (Sharepoint search for your site)

Same with Onedrive but select 1 instead of 4 (Onedrive personal)